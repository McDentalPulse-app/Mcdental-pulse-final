import React from 'react';

const Loader = () => (
  <div className="app-loader">
    <div className="app-loader-spinner" />
    <p className="app-loader-text">Cargando módulo...</p>
  </div>
);

export default Loader;
